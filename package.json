{
  "name": "fingerprint-proxy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cp package.json dist/package.json && cd dist && npm install --only=prod && nodemon -x \"clear; node --inspect=5858 index.js\""
  },
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "express-fingerprint": "^1.2.2"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.2",
    "@types/express": "^4.17.8",
    "nodemon": "^2.0.4",
    "typescript": "^4.0.3"
  }
}
